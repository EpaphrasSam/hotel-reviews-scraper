export interface Review {
  hotel: string;
  rating: string;
  date: string;
  stayDuration: string;
  content: string;
  reviewer: string;
  // userName: string;
  // userLocation: string;
  // stayDate: string;
  // travelerType: string;
  // roomInfo: string;
}
